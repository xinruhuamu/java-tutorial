1.HashMap
一种存储KV键值对，容量可变的非线程安全集合，JDK1.8之后，底层时数组+链表（链表为解决哈希碰撞），当链表的长度大于8时，链表转换为红黑树。
a.默认初始化容量为16，容量大小为2^n,每次扩容时增加2倍;默认加载因子为0.75
b.不能包含重复的键K，但值V可重复
c.允许K、V为null
d.无法保持数据存储时和取出时的顺序一致
e.KEY按照某种哈希函数计算得到，可能发生哈希碰撞导致出现相同的KEY，这也是数组+链表的原因，key相同时，链表存储后续值。

2.LinkedHashMap
是HashMap一种，LinkedHashMap可以保持数据存储时和取出时的顺序一致。
内部维持一个双向链表，维护元素插入顺序。
a.可以保持数据存储时和取出时的顺序一致

3.ConCurrentHashMap
HashMap一种，线程安全。

4.TreeMap
Key有序，非线程安全集合，底层是红黑树。
a.K、V均不能为null



